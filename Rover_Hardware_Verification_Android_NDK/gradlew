#!/bin/sh
# Standard lightweight Gradle runner for CI environments
if command -v gradle >/dev/null 2>&1; then
    exec gradle "$@"
else
    echo "Gradle wrapper: searching for gradle..."
    gradle "$@"
fi
